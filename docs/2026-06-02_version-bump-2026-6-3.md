# 2026-06-02 Version Bump 2026.6.3

## 작업 범위

- `ai_logger_core` 패키지 버전을 `2026.6.3`으로 올렸다.
- `ai_logger` 패키지 버전을 `2026.6.3`으로 올렸다.
- `ai_logger`의 `ai_logger_core` 의존성 제약을 `^2026.6.3`으로 올렸다.
- example 앱 버전을 `2026.6.3+2`로 올렸다.
- root/package CHANGELOG에 publish용 버전 bump를 기록했다.

## 목표 대응

- pub.dev에 이미 `2026.6.2`가 올라간 경우에도 새 패키지 버전으로 publish할 수 있다.
- Flutter example 앱도 새 패키지 버전과 build number를 함께 반영한다.
